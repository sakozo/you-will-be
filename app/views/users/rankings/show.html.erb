 <%= render partial: "template/page_top",  locals: {guide_message: "積み上げ時間ランキング　このアプリを使って取り組んでくださっているひとたち"}%>


<div class="message">

</div>
<br>
  
<div class="ranking_area">
<% @all_users.each.with_index(1) do |(user, total_time), index| %>
  <%= link_to user_path(user.id) ,class: "link" do %>
    <div class="ranking_panel">
    <%= index %>位  <%= user.name%>さん <%= total_time %>時間  <%= (total_time/Goal.find((user.goal_id)).time)*100.round(5) %>%<br>
    </div>
  <% end %>
<% end %>
</div>

<%= stylesheet_link_tag "book/ranking_page", :media => "all" %>