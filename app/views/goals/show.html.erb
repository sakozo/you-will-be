<%= stylesheet_link_tag "book/select_goal", :media => "all" %>
<h1>You will be...?</h1>
<div class="message">
<p>あなたがなりたいものを選んでください</p>
<p>一覧になければ自分で作ることもできます</p>
</div>

<%= form_for @user do |f| %>
  <% @goals.each do |goal| %>
    <label>
    <div><p><%= f.radio_button :goal_id, goal.id %><%=goal.name%>: <%=goal.time%>時間 <br>挑戦中:<%=goal.users.count%>人 <br><%= image_tag goal.image, :size =>'150x150' %></p></div>
    </label>
  <% end %>
<%= f.submit "決定!!" %>
<% end %>
<br><br>

<%= form_for @goal do |form| %>
  <%= form.text_field :name, placeholder: "なりたいもの" %>
  <%= form.text_field :time, placeholder: "かかる時間" %>
  <%= form.text_field :image, placeholder: "イメージ画像" %>
  <%= form.submit "SEND" %>
<% end %>
